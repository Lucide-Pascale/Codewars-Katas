<h2><a href=https://www.codewars.com/kata/52af1f150fcae8d33d0009bc/train/javascript target="_blank">Ho Ho Ho with Functions!</a></h2><h3>6 kyu</h3><p>Santa is learning programming. And what could be the first program, he wants to write? Yes, the "Hello world!" of Christmas: "Ho Ho Ho!". He wants to write a function <code>ho()</code>, which should have the following return values:</p><pre style="display: none;"><code class="language-java"><span class="cm-keyword">import</span> <span class="cm-keyword">static</span> <span class="cm-variable">christmas</span>.<span class="cm-variable">Santa</span>.<span class="cm-variable">ho</span>;...<span class="cm-variable">ho</span>();         <span class="cm-comment">// should return "Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>());     <span class="cm-comment">// should return "Ho Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())); <span class="cm-comment">// should return "Ho Ho Ho!"</span></code></pre><pre><code class="language-javascript"><span class="cm-variable">ho</span>();         <span class="cm-comment">// should return "Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>());     <span class="cm-comment">// should return "Ho Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())); <span class="cm-comment">// should return "Ho Ho Ho!"</span></code></pre><pre style="display: none;"><code class="language-python"><span class="cm-variable">ho</span>()         <span class="cm-comment"># should return "Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())     <span class="cm-comment"># should return "Ho Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())) <span class="cm-comment"># should return "Ho Ho Ho!"</span></code></pre><pre style="display: none;"><code class="language-haskell"><span class="cm-variable">ho</span>()         <span class="cm-comment">-- should return "Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())     <span class="cm-comment">-- should return "Ho Ho!"</span><span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>(<span class="cm-variable">ho</span>())) <span class="cm-comment">-- should return "Ho Ho Ho!"</span></code></pre><p>Unfortunately he couldn't find any tutorial, which explains, how he could implement that. Can you help him?</p><p>Rules:</p><ul><li>each call of <code>ho()</code> must add a "Ho" to the string</li><li>the "Ho"'s must be separated by a space</li><li>at the end of the string, there must be an exclamation mark (<code>!</code>), without a space</li></ul>